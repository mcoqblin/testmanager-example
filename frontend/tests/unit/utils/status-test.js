import status from 'frontend/utils/status';
import { module, test } from 'qunit';

module('Unit | Utility | status', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = status();
    assert.ok(result);
  });
});
