import status from 'frontend/utils/status-tools';
import { module, test } from 'qunit';

module('Unit | Utility | status-tools', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = status();
    assert.ok(result);
  });
});
