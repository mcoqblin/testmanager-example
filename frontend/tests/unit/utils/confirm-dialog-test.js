import confirmDialog from 'frontend/utils/confirm-dialog';
import { module, test } from 'qunit';

module('Unit | Utility | confirm-dialog', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = confirmDialog();
    assert.ok(result);
  });
});
