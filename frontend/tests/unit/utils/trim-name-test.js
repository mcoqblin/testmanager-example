import trimName from 'frontend/utils/trim-name';
import { module, test } from 'qunit';

module('Unit | Utility | trim-name', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = trimName();
    assert.ok(result);
  });
});
